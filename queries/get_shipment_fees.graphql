query GetShipmentFees($representations: [_Any!]!) {
    _entities(representations: $representations) {
        __typename
        ... on ShipmentMethod {
            calculateFees(items: $items)
        }
    }
}